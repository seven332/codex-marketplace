interface:
  display_name: "PR Check"
  short_description: "Inspect PR CI, reviews, and merge state."
  default_prompt: "Use $pr-check to inspect this pull request's current CI, review, and merge state."
