interface:
  display_name: "PR Workflow Loop"
  short_description: "Run and merge repeated PR workflow cycles."
  default_prompt: "Use $pr-workflow-loop to run and merge PR-sized workflow cycles until this scope is complete."
