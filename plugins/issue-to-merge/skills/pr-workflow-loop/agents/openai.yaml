interface:
  display_name: "PR Workflow Loop"
  short_description: "Deliver broad work through merged PR cycles."
  default_prompt: "Use $pr-workflow-loop to split and deliver this scope through merged PR-sized cycles until it is verified complete."
